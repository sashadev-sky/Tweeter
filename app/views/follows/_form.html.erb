<%# note on HTML documentation it says:

Tip: Always specify the type attribute for a <button> element. 
Different browsers use different default types for the <button> element.

they didnt do this here so just noting it  %>
<button class="follow-toggle"
  data-user-id="<%= user.id %>"
  data-initial-follow-state="<%= current_user.follows?(user) ? 'followed' : 'unfollowed' %>"></button>

<%# we stored data-user-id="<%#= user.id %> 
<%# in HTML for the button (passed in from feeds/show.html.erb), 
and we can retrieve it as el.data('user-id') %>